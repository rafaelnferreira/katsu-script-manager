{
  "name": "katsu-script-manager",
  "version": "1.0.0",
  "description": "",
  "main": "dist/src/app.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc",
    "start": "npm run build && node dist/src/app.js",
    "package": "npm run build && zip -r dist/katsu-script-manager.zip dist --exclude \"*.map\" && zip -r dist/katsu-script-manager.zip node_modules "
  },
  "author": "Silvio Piccolo",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "fs-extra": "^8.1.0",
    "hbs": "^4.1.0",
    "moment": "^2.24.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.2",
    "@types/node": "^13.7.0",
    "tslint": "^6.0.0",
    "typescript": "^3.7.5"
  }
}
